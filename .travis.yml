sudo: required

language: python

python:
  - "3.6"

install:
  - pip install -r requirements.txt
  - pip install -r requirements-integration.txt
  - pip install -r requirements-unit.txt

before_script:
  - python3 -m flask run

script:
  - python integration_test/test_example.py
